# Chapter 2 — companion code dependencies
#
# Verified against Python 3.11+. Run:
#   pip install -r requirements.txt
#
# Versions are pinned to the minor — patch updates are expected to be drop-in.
# For a frozen reproducible install (e.g. CI), regenerate with `pip freeze`.

# Core — used by every listing in section 2.2 and 2.3
google-genai~=1.74        # Gemini SDK with native structured output
pydantic~=2.13            # Schema validation for structured LLM responses

# Skeleton platform — section 2.7 (gateway, catalog, checkout, orders services)
fastapi~=0.136            # HTTP framework for the four services
uvicorn~=0.46             # ASGI server that runs the FastAPI apps
httpx~=0.28               # Async HTTP client used by the gateway proxy
asyncpg~=0.31             # Async Postgres driver for the catalog service
